{
  "name": "global-time-matrix",
  "private": true,
  "version": "1.0.0",
  "description": "Global Time Matrix — every timezone you work across, at a glance. A NavAse Technologies free app.",
  "type": "module",
  "author": "NavAse Technologies",
  "license": "SEE LICENSE IN LICENSE",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4319",
    "electron:dev": "vite build && electron .",
    "electron:build": "vite build && electron-builder --win --publish never"
  },
  "dependencies": {
    "luxon": "^3.5.0",
    "suncalc": "^1.9.0"
  },
  "devDependencies": {
    "@types/luxon": "^3.4.2",
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
